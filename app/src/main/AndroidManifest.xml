<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cice.imc">

    <application
        android:allowBackup="true"
        android:icon="@drawable/portada"
        android:label="@string/app_name"
        android:roundIcon="@drawable/portada"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ListaActivity" />
        <activity android:name=".listeners.DesnutridoActivity" />
        <activity android:name=".listeners.BajoPesoActivity" />
        <activity android:name=".listeners.NormalActivity" />
        <activity android:name=".listeners.SobrePesoActivity" />
        <activity android:name=".listeners.ObesoActivity"></activity>
    </application>

</manifest>